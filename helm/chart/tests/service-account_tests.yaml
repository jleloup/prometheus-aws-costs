---
suite: Test ServiceAccount resources
templates:
  - service-account.yaml
tests:
  - it: creates ServiceAccount resources
    set:
      serviceAccount:
        annotations:
          eks.amazonaws.com/role-arn: arn:aws:iam::FAKE_ACCOUNT:FAKE_IAM_ROLE
    asserts:
      - matchSnapshot: {}
